# MarkLogic -> Python -> Snowflake pipeline
requests>=2.32.0
python-dotenv>=1.0.0
snowflake-connector-python>=3.12.0
# only for ML_AUTH=kerberos
requests-negotiate-sspi>=0.5.2; sys_platform == "win32"
requests-kerberos>=0.15.0; sys_platform != "win32"
